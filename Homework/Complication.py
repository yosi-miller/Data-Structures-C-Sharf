# 4. Calculate the sum of all triplets in an array
# • Write a function SumOfAllTriplets(int[] array) that receives an array of integers
# and calculates the sum of all possible triplets in the array. The function should return the biggest
# triplet sum.
def sum_of_all_triplets(num: list) -> int:
    result: int = 0
    return result

a = [1, 2, 3]

print(type(a))